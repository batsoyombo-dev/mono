{
    "name": "procure",
    "devDependencies": {
        "@types/node": "^25.9.1",
        "prettier": "^3.7.4",
        "turbo": "^2.9.6",
        "typescript": "^6.0.3"
    },
    "catalog": {
        "zod": "^4.3.6",
        "bullmq": "^5.77.6",
        "dotenv": "^17.4.2"
    },
    "engines": {
        "node": ">=24"
    },
    "packageManager": "bun@1.3.4",
    "private": true,
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "dev:mq-worker": "turbo run dev --filter='mq-worker'",
        "db:generate": "turbo run db:generate",
        "db:migrate": "turbo run db:migrate",
        "lint": "turbo run lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\"",
        "check-types": "turbo run check-types"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ]
}
